Функция dup в Python будет выглядеть так:

```python
def dup(input_string):
    words = input_string.split()
    word_count = 0
    output_string = ""
    for word in words:
        if word == "word":
            word_count += 1
            if word_count % 2 == 0:
                word = "wordword"
        output_string += word + " "
    return output_string.strip()
```

Эта функция делит входную строку на слова, затем проходит по каждому слову и заменяет каждое второе вхождение слова "word" на "wordword".
