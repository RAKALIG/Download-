Даны стоимости конфет за килограмм в виде списка пар (артикул, цена). Также даны веса купленных покупателем конфет в виде списка пар (артикул, вес). Гарантируется, что для всех купленных конфет известна цена, а артикулы внутри одного списка не повторяются. Длины списков не превышают 10000.

Требуется написать функцию aggregate, принимающую на вход списки цен и покупок, и находящую минимальную стоимость по всем позициям из списка покупок.

Ввод/вывод в функции (и вне функции тоже) осуществлять не нужно.
На проверку нужно отправлять только код с функцией (без кода вызова/проверки функции).
Проверка осуществляется запуском вашего кода с последующим запуском кода теста.

 

Для примера:

Тест	
catalog = [(1, 2), (5, 10), (9, 4)]
cart = [(1, 20), (9, 1), (5, 1)]
print(aggregate(catalog, cart))

результат 4
