Реализовать функцию которой передается в качестве параметра две строки, а возвращается сколько различных знаков препинания встречается в первой строке, которых нет во второй строке. Для реализации функции использовать множество. 


Знаками препинания считаются символы " .,:!?'!"#$%&'()*+,-./:;<=>?@[\]^_`{|}~ " (пробелы не считаются).
