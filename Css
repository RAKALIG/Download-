Конечно! Вот пример программы на Python, которая находит углы треугольника по заданным сторонам:

import math

def find_angles(a, b, c):
    # Используем закон косинусов для нахождения углов
    angle_a = math.acos((b**2 + c**2 - a**2) / (2 * b * c))
    angle_b = math.acos((a**2 + c**2 - b**2) / (2 * a * c))
    angle_c = math.acos((a**2 + b**2 - c**2) / (2 * a * b))
    
    # Переводим углы из радиан в градусы
    angle_a = math.degrees(angle_a)
    angle_b = math.degrees(angle_b)
    angle_c = math.degrees(angle_c)
    
    return angle_a, angle_b, angle_c

# Ввод сторон треугольника
a = float(input("Введите длину стороны a: "))
b = float(input("Введите длину стороны b: "))
c = float(input("Введите длину стороны c: "))

# Находим углы треугольника
angle_a, angle_b, angle_c = find_angles(a, b, c)

# Выводим результат
print("Угол A =", angle_a)
print("Угол B =", angle_b)
print("Угол C =", angle_c)


При запуске программы вы будете приглашены ввести длины сторон треугольника, после чего программа выведет значения углов A, B и C треугольника.
